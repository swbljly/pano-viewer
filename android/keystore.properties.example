# 正式版签名配置模板
# 用法：复制本文件为 android/keystore.properties，并填入你自己的密钥信息。
# 注意：keystore.properties 含有密码，切勿提交到 Git（已在 .gitignore 建议中排除）。
#
# storeFile    : 你的签名密钥库(.jks/.keystore)的【绝对路径】或相对 android/ 的路径
# storePassword: 密钥库密码
# keyAlias     : 密钥别名
# keyPassword  : 密钥密码（若与密钥库密码相同可填一样）
#
# 首次没有密钥库？用下面的 keytool 命令生成一个（JDK 自带）：
#   keytool -genkeypair -v -keystore my-release-key.jks \
#           -keyalg RSA -keysize 2048 -validity 10000 \
#           -alias panoviewer -storepass 你的库密码 -keypass 你的密钥密码
#
# 然后把生成的 my-release-key.jks 放到 android/ 下，并按下面的示例填写本文件。

storeFile=my-release-key.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=panoviewer
keyPassword=YOUR_KEY_PASSWORD
